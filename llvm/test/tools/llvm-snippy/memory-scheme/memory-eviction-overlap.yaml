# REQUIRES: riscv-snippy-model-None
# RUN: not llvm-snippy %S/Inputs/default-layout.yaml %s \
# RUN:   -mtriple=riscv64 -num-instrs=100 \
# RUN:   |& FileCheck --dump-input always %s

access-evictions:
    - mask:  0x003f0000
      fixed: 0x80010000

# CHECK: memory-eviction-overlap.yaml:7:7: error: Bits in mask and fixed fields for eviction overlap
