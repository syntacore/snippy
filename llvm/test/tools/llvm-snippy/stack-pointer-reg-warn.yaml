# XFAIL: *
# RUN: llvm-snippy %s -march=riscv64-linux-gnu -redefine-sp=any-not-SP \
# RUN: -num-instrs=10000 -model-plugin=None -external-stack \
# RUN: |& FileCheck %s
# RUN: llvm-snippy %s -march=riscv64-linux-gnu -redefine-sp=any \
# RUN: -num-instrs=10000 -model-plugin=None -external-stack \
# RUN: |& FileCheck %s
sections:
    - no:        1
      VMA:       0x500000
      SIZE:      0x100000
      LMA:       0x500000
      ACCESS:    rx
    - no:        2
      VMA:       0x600000
      SIZE:      0x700000
      LMA:       0x600000
      ACCESS:    rw

histogram:
    - [ADD, 1.0]

# CHECK: warning: 'external-stack' is specified: 'redefine-sp' is assumed to be SP
