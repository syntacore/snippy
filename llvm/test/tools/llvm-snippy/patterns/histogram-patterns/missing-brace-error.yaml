# RUN: not llvm-snippy %s -model-plugin=None |& FileCheck %s

include:
  - Inputs/sections.yaml

options:
  mtriple: riscv64 
  num-instrs: 15

histogram-patterns:
  - MissingBrace: "(OR | AND) * (SUB | SLT"

# CHECK: error: expected ')' in 'MissingBrace' initialization
# CHECK-NEXT: - MissingBrace: "(OR | AND) * (SUB | SLT"
