# REQUIRES: riscv-snippy-model-None
# RUN: not llvm-snippy %s |& FileCheck %s

options:
  mtriple: riscv64 
  mattr: "+v"
  num-instrs: 10

include:
  - Inputs/sections.yaml

histogram-patterns:
  - Vector: "ADDI * VADD_VV"

# CHECK: error: vector instructions are not supported
