# XFAIL: *
# RUN: llvm-snippy %s --model-plugin None |& FileCheck %s

include:
  - Inputs/sections.yaml

options:
  march: riscv64-unknown-elf
  mcpu: generic-rv64
  mattr: "+d"
  num-instrs: 10
  dump-mf: true

histogram:
  - [FMUL_D, 1.0]

# CHECK: remark: Float override range was not specified
# CHECK-SAME: Using range: [{{-?[[:digit:]]+}}, {{-?[[:digit:]]+}}]
