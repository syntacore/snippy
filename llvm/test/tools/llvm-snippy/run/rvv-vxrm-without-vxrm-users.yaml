# REQUIRES: riscv-snippy-model-spike

# RUN: llvm-snippy %s |& FileCheck %s

options:
  mtriple: "riscv64"
  mattr: "+v"
  num-instrs: 1000

include:
- ../rvv-config-biased/Inputs/rvv-unit-all.yaml
- ../rvv-config-histogram/Inputs/sections.yaml

histogram:
    - [VADD_VV, 1.0]

# CHECK-NOT: csrwi   vxrm, {{[0-3]}}
