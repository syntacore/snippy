# REQUIRES: riscv-snippy-model-None
# RUN: not llvm-snippy %s \
# RUN: |& FileCheck %s

options:
  mtriple: riscv64
  external-stack: on
  enable-static-stack: on

include:
  - Inputs/sections-with-stack.yaml

# CHECK: Incompatible options: When external stack is provided, option -enable-static-stack is not supported.

