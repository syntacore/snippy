# RUN: not llvm-snippy %s -model-plugin None \
# RUN: |& FileCheck %s

options:
  march: riscv64-unknown-elf
  enable-static-stack: on

include:
  - Inputs/sections-with-stack.yaml

histogram:
  - [BEQ, 1.0]

# CHECK: error: Incompatible options: When loop generation is possible, option -enable-static-stack is not supported.
