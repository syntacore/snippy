#REQUIRES: this-test-requires-no-more-than-just-a-fix

# RUN: llvm-snippy %S/Inputs/rvv-vadd.yaml -mtriple=riscv64 -mattr=+v \
# RUN: -num-instrs=1000 -model-plugin=spike \
# RUN: |& FileCheck %s

# RUN: llvm-snippy %S/Inputs/rvv-many-opcodes.yaml -mtriple=riscv64 -mattr=+v \
# RUN: -num-instrs=1000 -model-plugin=spike \
# RUN: |& FileCheck %s


# CHECK-NOT: LLVM ERROR
include:
- Inputs/rvv-config.yaml

selfcheck:
  mode: code 
  selfcheck-td-options:
    enable-selfcheck-rvv: true
