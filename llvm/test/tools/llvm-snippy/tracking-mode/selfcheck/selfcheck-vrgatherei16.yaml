# UNSUPPORTED: riscv-snippy-model-None
# REQUIRES: riscv-rvv

# RUN: llvm-snippy %s \
# RUN:    -mtriple=riscv64 -mattr=+v \
# RUN:    -num-instrs=10 -init-regs-in-elf -selfcheck \
# RUN:    -enable-selfcheck-rvv -initial-regs-yaml=%S/Inputs/reg-init.yaml \
# RUN:    -rvv-init-mode=loads -o %t
# RUN: FileCheck %s --input-file=%t.selfcheck.yaml -check-prefix=ANNOTATION

include:
- "../../RISCV/rvv/rvv-config-histogram/Inputs/rvv-unit-sew_16-m1-max_encodable.yaml"
- Inputs/sections.yaml

histogram:
    - [VSETVL, 0.001]
    - [VRGATHEREI16_VV, 10000.0]

# ANNOTATION-COUNT-10: address
# ANNOTATION-NOT: address
