# UNSUPPORTED: riscv-snippy-model-None
# RUN: llvm-snippy %S/Inputs/rv-compressed.yaml \
# RUN:   -mtriple=riscv64 \
# RUN:   -num-instrs=10000 -init-regs-in-elf -verify-mi -mattr=+c,+f,+d \
# RUN:   -verify-gen-histogram -histogram-must-succeed=true \
# RUN:  |& FileCheck %s

# CHECK: #===Simulation Start===
