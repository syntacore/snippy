# REQUIRES: riscv-snippy-model-None
# RUN: llvm-snippy %s |& FileCheck %s

options:
  mtriple: riscv64
  march: rv64imfdv
  num-instrs: 1000
  dump-mf: on
  reserved-regs-list: ["V([2-9]|1[0-9]|2[0-9]|3[0-1])"]

include:
  - Inputs/sections.yaml
  - Inputs/rvv-unit-lmul1.yaml

histogram:
  - [VIOTA_M, 1.0]

# COM: Check that v0 can be destination.
# CHECK-DAG: $v1 = VIOTA_M $v0, $v0
# CHECK-DAG: $v0 = VIOTA_M $v1, $noreg
# CHECK-NOT: $v0 = VIOTA_M $v0, $v0
# CHECK-NOT: $v0 = VIOTA_M $v1, $v0
