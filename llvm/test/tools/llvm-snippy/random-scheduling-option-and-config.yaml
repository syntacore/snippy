# REQUIRES: riscv-snippy-model-None
# RUN: not llvm-snippy %s -random-scheduling |& FileCheck %s
# CHECK: random-scheduling-option-and-config.yaml:6:3: error: 'random-scheduling' has been specified both as an option and as a configuration field 'enabled'

scheduling:
  enabled: true
options:
  num-instrs: 10
  mtriple: riscv64
  mcpu: generic-rv64
