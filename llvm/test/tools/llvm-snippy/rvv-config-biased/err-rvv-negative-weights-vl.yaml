# RUN: not llvm-snippy %s -mtriple=riscv64 -mattr=+v \
# RUN: |& FileCheck %s

include:
- Inputs/err-rvv-negative-weights-vl.yaml

sections:
  - name:      1
    VMA:       0x80000000
    SIZE:      0x400000
    LMA:       0x80000000
    ACCESS:    rx
  - name:      2
    VMA:       0x80600000
    SIZE:      0x400000
    LMA:       0x80600000
    ACCESS:    rw
histogram:
    - [VADD_VV, 1.0]

# CHECK: error: incorrect weight specified for max_encodable: got '-1.0' while expecting a non-negative double value
